# 기획 → 개발 전달 프롬프트 — 2026-04-22 (2차)

> 이 문서를 개발 세션 시작 시 전달하세요.
> 1차 프롬프트(`PLAN-DEV-HANDOFF-20260422.md`)의 항목도 미반영이면 함께 처리.

---

## 개발 세션에 전달할 프롬프트

```
HANDOVER.md를 읽고 이어서 개발해줘.
아래 기획 변경 사항 반영 + 버그 수정을 요청해.
1차 전달 프롬프트(reports/PLAN-DEV-HANDOFF-20260422.md)의 미반영 항목도 함께 처리해줘.

## 기획 변경 반영 (10건)

### 1. 수정 마크(✎) 레이아웃 변경 — 3단계/5단계
- 기존: Pencil 아이콘이 타임스탬프와 텍스트 사이에 인라인 → 수정 블록과 미수정 블록의 텍스트 시작 위치가 달라짐
- 변경: 타임스탬프 색상 변경 + 타임스탬프 바로 아래 연필 아이콘
  - 수정된 블록 타임스탬프: `text-text-tertiary` → `text-primary`
  - 타임스탬프 아래에 Pencil 아이콘(10px, text-primary) 세로 배치
  - 타임스탬프+아이콘을 고정 폭 컨테이너(w-12) 안에서 세로 스택 → 본문 영역 크기 영향 제로
  - 미수정 블록: 타임스탬프만 (회색), 아이콘 없음
- Recording.tsx:625, Editing.tsx:597의 Pencil 위치를 타임스탬프 컨테이너 안으로 이동
- 상세: decisions.md "블록 구조 (좌측 슬롯)" 참조

### 2. 블록 텍스트 줄바꿈 렌더링
- 기존: <p>{block.text}</p>로 렌더링 → \n이 표시 안 됨
- 변경: <p> 태그에 `whitespace-pre-wrap` CSS 추가
- Recording.tsx:658, Editing.tsx:643의 <p> 클래스에 `whitespace-pre-wrap` 추가
- Summary.tsx는 이미 split('\n').map으로 처리 중이라 변경 불필요

### 3. 스레드 답장 로딩 표시 — 7단계
- 기존: "스레드 답장" 모드 전환 시 채널을 다시 선택해야 메시지 로딩됨
- 변경: 모드 전환 시 현재 선택된 채널의 메시지를 즉시 fetch
  - SendSave.tsx에서 sendMode가 'thread'로 변경될 때 listMessages 호출 추가
  - 로딩 중: 스피너 + "메시지 불러오는 중…" 표시
  - 빈 상태(0건)와 로딩 중을 시각적으로 구분
- 상세: decisions.md "스레드 선택 — 메시지 카드 UI" 참조

### 4. .md 저장 경로 변경 — 7단계
- 7단계 "로컬 저장" 섹션에 저장 경로 표시 + [변경] 버튼 추가
- [변경] 클릭 → 텍스트 입력으로 경로 수정 가능
- 변경된 경로는 해당 세션에만 적용 (설정 기본값 불변)
- 기본값은 설정의 export_path에서 가져옴
- 상세: decisions.md 7단계 "로컬 저장" 참조

### 5. 뒤로가기 버튼 전 단계 확인
- 5단계(편집) → 3단계(녹음)로 이동 가능하도록 변경 (기존: 비활성)
- 3단계에 post_recording 상태로 진입 → 녹음 추가 가능
- 모든 단계에서 이전 버튼이 동작하는지 전수 확인:
  - 2→1, 3→2(녹음 중지 후), 5→3, 6→5, 7→6 모두 동작해야 함
  - 4단계만 버튼 숨김 (처리 중 이탈 차단)
- 상세: decisions.md "[← 이전 단계] 버튼 단계별 동작" 테이블 참조

### 6. Whisper 처리 스킵 옵션 — 4단계
- 4단계 진입 시 처리 시작 전에 선택지 표시:
  - [Whisper 처리 시작]: 기존 플로우
  - [건너뛰기]: Web Speech 블록 그대로 5단계로 이동 (AI 태깅은 별도 실행)
- 업로드 경로에서는 건너뛰기 불가 (버튼 숨김, 자동 처리 시작)
- 상세: decisions.md 4단계 "Whisper 처리 선택 화면" 참조

### 7. 복구 시 기존 전사 표시
- 복구 진입(3단계) 시 GET /api/sessions/{id}로 기존 블록 로드하여 전사 영역에 표시
- post_recording 상태로 진입
- 상단에 인라인 배너: "이전 녹음이 복원되었습니다. 이어서 녹음하거나 다음 단계로 진행하세요." (닫기 가능)
- 상세: decisions.md 복구 UX "3단계 복구 진입 시 필수 동작" 참조

### 8. 요약 생략 옵션 — 5→6 전환
- 5단계 [다음 단계] 클릭 시 모달: [요약 생성] / [건너뛰고 전송으로]
- [건너뛰기]: 6단계 스킵, 5→7 직행
  - summary_markdown = null인 경우 7단계에서 "요약 없음" 표시
  - Slack 메시지에서 "핵심 요약" 섹션 생략
  - .md export에도 요약 섹션 없이 전사 원본만 포함
- 상세: decisions.md 6단계 "요약 생성 타이밍" 참조

### 9. 마이크 민감도 조절 — 3단계 + 설정
- 3단계: 회의 정보 패널 안에 슬라이더 추가 (범위 0.5x~3.0x, 패널 접히면 안 보임)
  - Web Audio GainNode로 오디오 스트림 증폭 후 MediaRecorder에 전달
  - 녹음 중에도 실시간 조절 가능 (GainNode.gain 값 즉시 반영)
- 설정 화면: "연동" 섹션에 "마이크 민감도" 슬라이더 추가 (기본값 설정)
  - settings.json에 mic_sensitivity 필드 추가 (기본 1.0)
  - 3단계 진입 시 설정값을 초기값으로 사용
- 상세: decisions.md 3단계 "회의 정보 요약" + 설정 화면 참조

### 10. 녹음 컨트롤 FAB — 3단계
- 기존 sticky bottom 컨트롤 바를 Floating Action Button으로 변경:
  - 위치: `fixed bottom-24 right-6` — 하단 네비게이션([← 이전][다음 →])과 겹치지 않도록 96px 간격
  - recording 상태: 빨간 원형(56x56, bg-recording, shadow-lg) — ■ 중지
  - post_recording: 초록 원형(56x56, bg-success, shadow-lg) — ● 재개
  - idle: FAB 없음 — 녹음 시작 버튼은 전사 영역 위 원래 위치
  - position: fixed → 스크롤과 완전 독립
- 상세: decisions.md 3단계 레이아웃 참조

---

## 디자인 변경 (1건)

### D1. 입력 필드 배경색 — 배경 블록 안에서 겹치는 문제
- 원칙: 입력 필드는 항상 부모 배경보다 한 단계 대비되는 색
- 부모가 bg-bg(white) → 입력 bg-bg-subtle (기존, 유지)
- 부모가 bg-bg-subtle(연회색) → 입력 **bg-bg(white)** + `ring-1 ring-border-light`
- 다크모드도 동일 원칙 적용
- 전체 컴포넌트에서 bg-bg-subtle 안에 bg-bg-subtle 입력 필드가 있는 곳을 grep해서 교체
- 상세: design-system.md "입력 필드 배경색 원칙" 참조

---

## 버그 수정 (1건)

### B1. 녹음 중 분할/합치기 동작 확인 (#3)
- 코드에 이미 구현되어 있으나(Recording.tsx:634-649), 실제 녹음 중 동작하는지 확인
- 특히 녹음 중(recording 상태) + 편집 모드에서 Ctrl+Enter(분할), Backspace/Delete(합치기)가 정상 동작하는지 테스트
- 문제 있으면 수정, 없으면 확인 완료로 보고

---

## 1차 프롬프트 미반영 확인 (reports/PLAN-DEV-HANDOFF-20260422.md)

아래 항목이 아직 미반영이면 이번에 함께 처리:
- AI 태깅/요약 진행 표시
- 키보드 명령어 변경 (Shift+Enter→줄바꿈, Ctrl+Enter→분할)
- 섹션 제목-콘텐츠 위계 강화 (서브텍스트 + 배경 블록)
- Slack 인사 문구 textarea
- 히스토리 회의록 삭제 (DELETE /api/meetings/{id})
- 7단계 미리보기 실시간 반영
- 버그 7건 (Slack 유저 ID, 재편집 404, API 설정 표시, 복구 정보, 3→5 미반영, 첫 블록 에러, Slack 메시지 미반영)

세션 종료 시 reports/DEV-REPORT-20260422.md 작성 부탁해 (HANDOVER.md 11절 양식 참조).
```
